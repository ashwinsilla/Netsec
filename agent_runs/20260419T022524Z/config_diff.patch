diff --git a/intended/configs/dc1-leaf1a.cfg b/intended/configs/dc1-leaf1a.cfg
index 97b54d3..78dd060 100644
--- a/intended/configs/dc1-leaf1a.cfg
+++ b/intended/configs/dc1-leaf1a.cfg
@@ -23,6 +23,9 @@ spanning-tree mode rstp
 no spanning-tree vlan-id 4093-4094
 spanning-tree priority 8192
 !
+vlan 10
+   name VLAN10
+!
 vlan 11
    name VRF10_VLAN11
 !
@@ -90,7 +93,7 @@ interface Port-Channel5
 interface Port-Channel8
    description L2_dc1-leaf1c_Port-Channel1
    no shutdown
-   switchport trunk allowed vlan 11-12,21-22,3401-3402
+   switchport trunk allowed vlan 10-12,21-22,3401-3402
    switchport mode trunk
    switchport
    mlag 8
@@ -157,6 +160,11 @@ interface Management1
    vrf MGMT
    ip address 172.16.1.101/24
 !
+interface Vlan10
+   description VLAN10
+   no shutdown
+   ip address virtual 10.10.10.1/24
+!
 interface Vlan11
    description VRF10_VLAN11
    no shutdown
@@ -213,12 +221,14 @@ interface Vxlan1
    vxlan source-interface Loopback1
    vxlan virtual-router encapsulation mac-address mlag-system-id
    vxlan udp-port 4789
+   vxlan vlan 10 vni 10010
    vxlan vlan 11 vni 10011
    vxlan vlan 12 vni 10012
    vxlan vlan 21 vni 10021
    vxlan vlan 22 vni 10022
    vxlan vlan 3401 vni 13401
    vxlan vlan 3402 vni 13402
+   vxlan vrf default vni 1
    vxlan vrf VRF10 vni 10
    vxlan vrf VRF11 vni 11
 !
@@ -239,6 +249,9 @@ ip prefix-list PL-LOOPBACKS-EVPN-OVERLAY
 ip prefix-list PL-MLAG-PEER-VRFS
    seq 10 permit 10.255.1.96/31
 !
+ip prefix-list PL-SVI-VRF-DEFAULT
+   seq 10 permit 10.10.10.0/24
+!
 mlag configuration
    domain-id DC1_L3_LEAF1
    local-interface Vlan4094
@@ -252,14 +265,25 @@ ip route vrf MGMT 0.0.0.0/0 172.16.1.1
 ntp local-interface vrf MGMT Management1
 ntp server vrf MGMT 0.pool.ntp.org prefer
 !
+route-map RM-BGP-UNDERLAY-PEERS-OUT deny 10
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
+route-map RM-BGP-UNDERLAY-PEERS-OUT permit 20
+!
 route-map RM-CONN-2-BGP permit 10
    match ip address prefix-list PL-LOOPBACKS-EVPN-OVERLAY
 !
+route-map RM-CONN-2-BGP permit 30
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
 route-map RM-CONN-2-BGP-VRFS deny 10
    match ip address prefix-list PL-MLAG-PEER-VRFS
 !
 route-map RM-CONN-2-BGP-VRFS permit 20
 !
+route-map RM-EVPN-EXPORT-VRF-DEFAULT permit 10
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
 route-map RM-MLAG-PEER-IN permit 10
    description Make routes learned over MLAG Peer-link less preferred on spines to ensure optimal routing
    set origin incomplete
@@ -279,6 +303,7 @@ router bgp 65101
    neighbor EVPN-OVERLAY-PEERS send-community
    neighbor EVPN-OVERLAY-PEERS maximum-routes 0
    neighbor IPv4-UNDERLAY-PEERS peer group
+   neighbor IPv4-UNDERLAY-PEERS route-map RM-BGP-UNDERLAY-PEERS-OUT out
    neighbor IPv4-UNDERLAY-PEERS password 7 7x4B4rnJhZB438m9+BrBfQ==
    neighbor IPv4-UNDERLAY-PEERS send-community
    neighbor IPv4-UNDERLAY-PEERS maximum-routes 256000
@@ -306,6 +331,11 @@ router bgp 65101
    neighbor 10.255.255.2 description dc1-spine2_Ethernet1
    redistribute connected route-map RM-CONN-2-BGP
    !
+   vlan 10
+      rd 10.255.0.3:10010
+      route-target both 10010:10010
+      redistribute learned
+   !
    vlan 11
       rd 10.255.0.3:10011
       route-target both 10011:10011
@@ -344,6 +374,12 @@ router bgp 65101
       neighbor IPv4-UNDERLAY-PEERS activate
       neighbor MLAG-IPv4-UNDERLAY-PEER activate
    !
+   vrf default
+      rd 10.255.0.3:1
+      route-target import evpn 1:1
+      route-target export evpn 1:1
+      route-target export evpn route-map RM-EVPN-EXPORT-VRF-DEFAULT
+   !
    vrf VRF10
       rd 10.255.0.3:10
       route-target import evpn 10:10
diff --git a/intended/configs/dc1-leaf1b.cfg b/intended/configs/dc1-leaf1b.cfg
index 91f22fa..6071e0d 100644
--- a/intended/configs/dc1-leaf1b.cfg
+++ b/intended/configs/dc1-leaf1b.cfg
@@ -23,6 +23,9 @@ spanning-tree mode rstp
 no spanning-tree vlan-id 4093-4094
 spanning-tree priority 8192
 !
+vlan 10
+   name VLAN10
+!
 vlan 11
    name VRF10_VLAN11
 !
@@ -90,7 +93,7 @@ interface Port-Channel5
 interface Port-Channel8
    description L2_dc1-leaf1c_Port-Channel1
    no shutdown
-   switchport trunk allowed vlan 11-12,21-22,3401-3402
+   switchport trunk allowed vlan 10-12,21-22,3401-3402
    switchport mode trunk
    switchport
    mlag 8
@@ -157,6 +160,11 @@ interface Management1
    vrf MGMT
    ip address 172.16.1.102/24
 !
+interface Vlan10
+   description VLAN10
+   no shutdown
+   ip address virtual 10.10.10.1/24
+!
 interface Vlan11
    description VRF10_VLAN11
    no shutdown
@@ -213,12 +221,14 @@ interface Vxlan1
    vxlan source-interface Loopback1
    vxlan virtual-router encapsulation mac-address mlag-system-id
    vxlan udp-port 4789
+   vxlan vlan 10 vni 10010
    vxlan vlan 11 vni 10011
    vxlan vlan 12 vni 10012
    vxlan vlan 21 vni 10021
    vxlan vlan 22 vni 10022
    vxlan vlan 3401 vni 13401
    vxlan vlan 3402 vni 13402
+   vxlan vrf default vni 1
    vxlan vrf VRF10 vni 10
    vxlan vrf VRF11 vni 11
 !
@@ -239,6 +249,9 @@ ip prefix-list PL-LOOPBACKS-EVPN-OVERLAY
 ip prefix-list PL-MLAG-PEER-VRFS
    seq 10 permit 10.255.1.96/31
 !
+ip prefix-list PL-SVI-VRF-DEFAULT
+   seq 10 permit 10.10.10.0/24
+!
 mlag configuration
    domain-id DC1_L3_LEAF1
    local-interface Vlan4094
@@ -252,14 +265,25 @@ ip route vrf MGMT 0.0.0.0/0 172.16.1.1
 ntp local-interface vrf MGMT Management1
 ntp server vrf MGMT 0.pool.ntp.org prefer
 !
+route-map RM-BGP-UNDERLAY-PEERS-OUT deny 10
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
+route-map RM-BGP-UNDERLAY-PEERS-OUT permit 20
+!
 route-map RM-CONN-2-BGP permit 10
    match ip address prefix-list PL-LOOPBACKS-EVPN-OVERLAY
 !
+route-map RM-CONN-2-BGP permit 30
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
 route-map RM-CONN-2-BGP-VRFS deny 10
    match ip address prefix-list PL-MLAG-PEER-VRFS
 !
 route-map RM-CONN-2-BGP-VRFS permit 20
 !
+route-map RM-EVPN-EXPORT-VRF-DEFAULT permit 10
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
 route-map RM-MLAG-PEER-IN permit 10
    description Make routes learned over MLAG Peer-link less preferred on spines to ensure optimal routing
    set origin incomplete
@@ -279,6 +303,7 @@ router bgp 65101
    neighbor EVPN-OVERLAY-PEERS send-community
    neighbor EVPN-OVERLAY-PEERS maximum-routes 0
    neighbor IPv4-UNDERLAY-PEERS peer group
+   neighbor IPv4-UNDERLAY-PEERS route-map RM-BGP-UNDERLAY-PEERS-OUT out
    neighbor IPv4-UNDERLAY-PEERS password 7 7x4B4rnJhZB438m9+BrBfQ==
    neighbor IPv4-UNDERLAY-PEERS send-community
    neighbor IPv4-UNDERLAY-PEERS maximum-routes 256000
@@ -306,6 +331,11 @@ router bgp 65101
    neighbor 10.255.255.6 description dc1-spine2_Ethernet2
    redistribute connected route-map RM-CONN-2-BGP
    !
+   vlan 10
+      rd 10.255.0.4:10010
+      route-target both 10010:10010
+      redistribute learned
+   !
    vlan 11
       rd 10.255.0.4:10011
       route-target both 10011:10011
@@ -344,6 +374,12 @@ router bgp 65101
       neighbor IPv4-UNDERLAY-PEERS activate
       neighbor MLAG-IPv4-UNDERLAY-PEER activate
    !
+   vrf default
+      rd 10.255.0.4:1
+      route-target import evpn 1:1
+      route-target export evpn 1:1
+      route-target export evpn route-map RM-EVPN-EXPORT-VRF-DEFAULT
+   !
    vrf VRF10
       rd 10.255.0.4:10
       route-target import evpn 10:10
diff --git a/intended/configs/dc1-leaf1c.cfg b/intended/configs/dc1-leaf1c.cfg
index 1f47976..7d427b8 100644
--- a/intended/configs/dc1-leaf1c.cfg
+++ b/intended/configs/dc1-leaf1c.cfg
@@ -22,6 +22,9 @@ ip name-server vrf MGMT 192.168.1.1
 spanning-tree mode mstp
 spanning-tree mst 0 priority 32768
 !
+vlan 10
+   name VLAN10
+!
 vlan 11
    name VRF10_VLAN11
 !
@@ -52,7 +55,7 @@ management api http-commands
 interface Port-Channel1
    description L2_DC1_L3_LEAF1_Port-Channel8
    no shutdown
-   switchport trunk allowed vlan 11-12,21-22,3401-3402
+   switchport trunk allowed vlan 10-12,21-22,3401-3402
    switchport mode trunk
    switchport
 !
diff --git a/intended/configs/dc1-leaf2a.cfg b/intended/configs/dc1-leaf2a.cfg
index 007c9d7..30a2d0f 100644
--- a/intended/configs/dc1-leaf2a.cfg
+++ b/intended/configs/dc1-leaf2a.cfg
@@ -23,6 +23,9 @@ spanning-tree mode rstp
 no spanning-tree vlan-id 4093-4094
 spanning-tree priority 8192
 !
+vlan 10
+   name VLAN10
+!
 vlan 11
    name VRF10_VLAN11
 !
@@ -90,7 +93,7 @@ interface Port-Channel5
 interface Port-Channel8
    description L2_dc1-leaf2c_Port-Channel1
    no shutdown
-   switchport trunk allowed vlan 11-12,21-22,3401-3402
+   switchport trunk allowed vlan 10-12,21-22,3401-3402
    switchport mode trunk
    switchport
    mlag 8
@@ -157,6 +160,11 @@ interface Management1
    vrf MGMT
    ip address 172.16.1.103/24
 !
+interface Vlan10
+   description VLAN10
+   no shutdown
+   ip address virtual 10.10.10.1/24
+!
 interface Vlan11
    description VRF10_VLAN11
    no shutdown
@@ -213,12 +221,14 @@ interface Vxlan1
    vxlan source-interface Loopback1
    vxlan virtual-router encapsulation mac-address mlag-system-id
    vxlan udp-port 4789
+   vxlan vlan 10 vni 10010
    vxlan vlan 11 vni 10011
    vxlan vlan 12 vni 10012
    vxlan vlan 21 vni 10021
    vxlan vlan 22 vni 10022
    vxlan vlan 3401 vni 13401
    vxlan vlan 3402 vni 13402
+   vxlan vrf default vni 1
    vxlan vrf VRF10 vni 10
    vxlan vrf VRF11 vni 11
 !
@@ -239,6 +249,9 @@ ip prefix-list PL-LOOPBACKS-EVPN-OVERLAY
 ip prefix-list PL-MLAG-PEER-VRFS
    seq 10 permit 10.255.1.100/31
 !
+ip prefix-list PL-SVI-VRF-DEFAULT
+   seq 10 permit 10.10.10.0/24
+!
 mlag configuration
    domain-id DC1_L3_LEAF2
    local-interface Vlan4094
@@ -252,14 +265,25 @@ ip route vrf MGMT 0.0.0.0/0 172.16.1.1
 ntp local-interface vrf MGMT Management1
 ntp server vrf MGMT 0.pool.ntp.org prefer
 !
+route-map RM-BGP-UNDERLAY-PEERS-OUT deny 10
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
+route-map RM-BGP-UNDERLAY-PEERS-OUT permit 20
+!
 route-map RM-CONN-2-BGP permit 10
    match ip address prefix-list PL-LOOPBACKS-EVPN-OVERLAY
 !
+route-map RM-CONN-2-BGP permit 30
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
 route-map RM-CONN-2-BGP-VRFS deny 10
    match ip address prefix-list PL-MLAG-PEER-VRFS
 !
 route-map RM-CONN-2-BGP-VRFS permit 20
 !
+route-map RM-EVPN-EXPORT-VRF-DEFAULT permit 10
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
 route-map RM-MLAG-PEER-IN permit 10
    description Make routes learned over MLAG Peer-link less preferred on spines to ensure optimal routing
    set origin incomplete
@@ -279,6 +303,7 @@ router bgp 65102
    neighbor EVPN-OVERLAY-PEERS send-community
    neighbor EVPN-OVERLAY-PEERS maximum-routes 0
    neighbor IPv4-UNDERLAY-PEERS peer group
+   neighbor IPv4-UNDERLAY-PEERS route-map RM-BGP-UNDERLAY-PEERS-OUT out
    neighbor IPv4-UNDERLAY-PEERS password 7 7x4B4rnJhZB438m9+BrBfQ==
    neighbor IPv4-UNDERLAY-PEERS send-community
    neighbor IPv4-UNDERLAY-PEERS maximum-routes 256000
@@ -306,6 +331,11 @@ router bgp 65102
    neighbor 10.255.255.10 description dc1-spine2_Ethernet3
    redistribute connected route-map RM-CONN-2-BGP
    !
+   vlan 10
+      rd 10.255.0.5:10010
+      route-target both 10010:10010
+      redistribute learned
+   !
    vlan 11
       rd 10.255.0.5:10011
       route-target both 10011:10011
@@ -344,6 +374,12 @@ router bgp 65102
       neighbor IPv4-UNDERLAY-PEERS activate
       neighbor MLAG-IPv4-UNDERLAY-PEER activate
    !
+   vrf default
+      rd 10.255.0.5:1
+      route-target import evpn 1:1
+      route-target export evpn 1:1
+      route-target export evpn route-map RM-EVPN-EXPORT-VRF-DEFAULT
+   !
    vrf VRF10
       rd 10.255.0.5:10
       route-target import evpn 10:10
diff --git a/intended/configs/dc1-leaf2b.cfg b/intended/configs/dc1-leaf2b.cfg
index a2625a9..76d4eff 100644
--- a/intended/configs/dc1-leaf2b.cfg
+++ b/intended/configs/dc1-leaf2b.cfg
@@ -23,6 +23,9 @@ spanning-tree mode rstp
 no spanning-tree vlan-id 4093-4094
 spanning-tree priority 8192
 !
+vlan 10
+   name VLAN10
+!
 vlan 11
    name VRF10_VLAN11
 !
@@ -90,7 +93,7 @@ interface Port-Channel5
 interface Port-Channel8
    description L2_dc1-leaf2c_Port-Channel1
    no shutdown
-   switchport trunk allowed vlan 11-12,21-22,3401-3402
+   switchport trunk allowed vlan 10-12,21-22,3401-3402
    switchport mode trunk
    switchport
    mlag 8
@@ -157,6 +160,11 @@ interface Management1
    vrf MGMT
    ip address 172.16.1.104/24
 !
+interface Vlan10
+   description VLAN10
+   no shutdown
+   ip address virtual 10.10.10.1/24
+!
 interface Vlan11
    description VRF10_VLAN11
    no shutdown
@@ -213,12 +221,14 @@ interface Vxlan1
    vxlan source-interface Loopback1
    vxlan virtual-router encapsulation mac-address mlag-system-id
    vxlan udp-port 4789
+   vxlan vlan 10 vni 10010
    vxlan vlan 11 vni 10011
    vxlan vlan 12 vni 10012
    vxlan vlan 21 vni 10021
    vxlan vlan 22 vni 10022
    vxlan vlan 3401 vni 13401
    vxlan vlan 3402 vni 13402
+   vxlan vrf default vni 1
    vxlan vrf VRF10 vni 10
    vxlan vrf VRF11 vni 11
 !
@@ -239,6 +249,9 @@ ip prefix-list PL-LOOPBACKS-EVPN-OVERLAY
 ip prefix-list PL-MLAG-PEER-VRFS
    seq 10 permit 10.255.1.100/31
 !
+ip prefix-list PL-SVI-VRF-DEFAULT
+   seq 10 permit 10.10.10.0/24
+!
 mlag configuration
    domain-id DC1_L3_LEAF2
    local-interface Vlan4094
@@ -252,14 +265,25 @@ ip route vrf MGMT 0.0.0.0/0 172.16.1.1
 ntp local-interface vrf MGMT Management1
 ntp server vrf MGMT 0.pool.ntp.org prefer
 !
+route-map RM-BGP-UNDERLAY-PEERS-OUT deny 10
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
+route-map RM-BGP-UNDERLAY-PEERS-OUT permit 20
+!
 route-map RM-CONN-2-BGP permit 10
    match ip address prefix-list PL-LOOPBACKS-EVPN-OVERLAY
 !
+route-map RM-CONN-2-BGP permit 30
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
 route-map RM-CONN-2-BGP-VRFS deny 10
    match ip address prefix-list PL-MLAG-PEER-VRFS
 !
 route-map RM-CONN-2-BGP-VRFS permit 20
 !
+route-map RM-EVPN-EXPORT-VRF-DEFAULT permit 10
+   match ip address prefix-list PL-SVI-VRF-DEFAULT
+!
 route-map RM-MLAG-PEER-IN permit 10
    description Make routes learned over MLAG Peer-link less preferred on spines to ensure optimal routing
    set origin incomplete
@@ -279,6 +303,7 @@ router bgp 65102
    neighbor EVPN-OVERLAY-PEERS send-community
    neighbor EVPN-OVERLAY-PEERS maximum-routes 0
    neighbor IPv4-UNDERLAY-PEERS peer group
+   neighbor IPv4-UNDERLAY-PEERS route-map RM-BGP-UNDERLAY-PEERS-OUT out
    neighbor IPv4-UNDERLAY-PEERS password 7 7x4B4rnJhZB438m9+BrBfQ==
    neighbor IPv4-UNDERLAY-PEERS send-community
    neighbor IPv4-UNDERLAY-PEERS maximum-routes 256000
@@ -306,6 +331,11 @@ router bgp 65102
    neighbor 10.255.255.14 description dc1-spine2_Ethernet4
    redistribute connected route-map RM-CONN-2-BGP
    !
+   vlan 10
+      rd 10.255.0.6:10010
+      route-target both 10010:10010
+      redistribute learned
+   !
    vlan 11
       rd 10.255.0.6:10011
       route-target both 10011:10011
@@ -344,6 +374,12 @@ router bgp 65102
       neighbor IPv4-UNDERLAY-PEERS activate
       neighbor MLAG-IPv4-UNDERLAY-PEER activate
    !
+   vrf default
+      rd 10.255.0.6:1
+      route-target import evpn 1:1
+      route-target export evpn 1:1
+      route-target export evpn route-map RM-EVPN-EXPORT-VRF-DEFAULT
+   !
    vrf VRF10
       rd 10.255.0.6:10
       route-target import evpn 10:10
diff --git a/intended/configs/dc1-leaf2c.cfg b/intended/configs/dc1-leaf2c.cfg
index adc3f1d..2b33fda 100644
--- a/intended/configs/dc1-leaf2c.cfg
+++ b/intended/configs/dc1-leaf2c.cfg
@@ -22,6 +22,9 @@ ip name-server vrf MGMT 192.168.1.1
 spanning-tree mode mstp
 spanning-tree mst 0 priority 32768
 !
+vlan 10
+   name VLAN10
+!
 vlan 11
    name VRF10_VLAN11
 !
@@ -52,7 +55,7 @@ management api http-commands
 interface Port-Channel1
    description L2_DC1_L3_LEAF2_Port-Channel8
    no shutdown
-   switchport trunk allowed vlan 11-12,21-22,3401-3402
+   switchport trunk allowed vlan 10-12,21-22,3401-3402
    switchport mode trunk
    switchport
 !