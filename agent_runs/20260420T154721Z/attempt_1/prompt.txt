<merge_target>
context_file: group_vars/DC1_L3_LEAVES/l3_leaves.yml
insertion_path: ["l3leaf","defaults","structured_config","static_routes"]
The tool walks the YAML root following insertion_path and replaces exactly one value with your JSON output.
Shape rules:
  (1) scalar field  → output the raw JSON scalar value.
  (2) dict field    → output a JSON object `{...}`.
  (3) list field    → output a JSON array `[...]` with ALL existing items plus your additions.
Do NOT echo the top-level file key or content outside the replaced subtree.
</merge_target>

<configuration_context source="group_vars/DC1_L3_LEAVES/l3_leaves.yml" section="l3leaf">
l3leaf:
  defaults:
    platform: cEOSLab
    loopback_ipv4_pool: 10.255.0.0/27
    loopback_ipv4_offset: 2
    vtep_loopback_ipv4_pool: 10.255.1.0/27
    uplink_switches:
    - dc1-spine1
    - dc1-spine2
    uplink_ipv4_pool: 10.255.255.0/26
    mlag_peer_ipv4_pool: 10.255.1.64/27
    mlag_peer_l3_ipv4_pool: 10.255.1.96/27
    virtual_router_mac_address: 00:1c:73:00:00:99
    spanning_tree_priority: 8192
    spanning_tree_mode: rstp
  node_groups:
  - group: DC1_L3_LEAF1
    bgp_as: 65101
    nodes:
    - name: dc1-leaf1a
      id: 1
      mgmt_ip: 172.16.1.101/24
    - name: dc1-leaf1b
      id: 2
      mgmt_ip: 172.16.1.102/24
  - group: DC1_L3_LEAF2
    bgp_as: 65102
    nodes:
    - name: dc1-leaf2a
      id: 3
      mgmt_ip: 172.16.1.103/24
    - name: dc1-leaf2b
      id: 4
      mgmt_ip: 172.16.1.104/24
</configuration_context>

<task>
Under l3leaf.defaults.structured_config.static_routes, add a new list (creating structured_config if it does not already exist) containing one entry: {destination_address_prefix: '0.0.0.0/0', gateway: '10.255.255.1'}. This uses the eos_cli_config_gen format required for static routes applied to all leaf nodes via structured_config.
</task>

Before generating the JSON, open a <thinking> block to map the request to exact schema field names, verify required nesting layers, and check all keys are valid. After closing your </thinking> block, output the JSON.

Subtree rule: output ONLY the JSON subtree that replaces the merge target (for list targets, include the full merged list after your edits).

Remember: Output ONLY the raw JSON immediately after </thinking>. Any non-JSON text causes a fatal parse error.