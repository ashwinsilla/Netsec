<thinking>
The user wants to add VLAN 90 on leaf nodes. In AVD, VLANs are defined in the network services file under tenants/VRFs or as L2 VLANs. This belongs in the network_services.yml file under the tenants structure.

Actually, let me think more carefully. Adding a VLAN to leaf nodes in AVD context - VLANs are typically defined in network_services.yml as either:
1. L2 VLANs (under tenants -> l2vlans)
2. SVIs within VRFs (under tenants -> vrfs -> svis)

Since the user just says "add vlan 90 on leaf nodes" without specifying a VRF or SVI, this is likely an L2 VLAN addition to the network services file.

The insertion path would be into the tenants list's l2vlans section. Since I don't have the full structure of network_services.yml, I'll point to the tenants level and note it should be added as an l2vlan.

Actually, the insertion_path should point to where the VLAN should be added. For L2 VLANs in AVD, the path would be ["tenants", 0, "l2vlans"] - appending to the l2vlans list of the first tenant.

The file is netsvcs (group_vars/NETWORK_SERVICES/network_services.yml).

Confidence: medium - because we don't know if it should be an L2 VLAN or an SVI in a VRF, and we don't have the full structure of the netsvcs file.
</thinking>

{
  "file_key":       "netsvcs",
  "context_file":   "group_vars/NETWORK_SERVICES/network_services.yml",
  "insertion_path": ["tenants", 0, "l2vlans"],
  "task_text":      "Append a new L2 VLAN entry with id 90 and name 'VLAN90' to the l2vlans list of the first tenant. Use AVD l2vlans schema: {id: 90, name: 'VLAN90'}. This will provision VLAN 90 on all leaf nodes that are members of this tenant.",
  "confidence":     "medium",
  "reasoning":      "VLANs for leaf nodes are defined in network_services.yml under tenants; a plain VLAN with no SVI/routing requirement belongs in the tenant's l2vlans list."
}